{
  "name": "game-server-demo",
  "version": "1.0.0",
  "description": "MMO-Lite 游戏服务器 Demo - Node.js 游戏后台教程",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "prestart": "lsof -ti:${PORT:-4000} | xargs kill -9 2>/dev/null || true",
    "start": "ts-node src/server.ts",
    "predev": "lsof -ti:${PORT:-4000} | xargs kill -9 2>/dev/null || true",
    "dev": "ts-node --watch src/server.ts",
    "stop": "lsof -ti:${PORT:-4000} | xargs kill -9 2>/dev/null || true"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.13",
    "@types/node": "^22.10.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.0"
  }
}
